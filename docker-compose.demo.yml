version: '3.8'

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: hayden
      POSTGRES_PASSWORD: hayden123
      POSTGRES_DB: squeezeweasel_demo
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

